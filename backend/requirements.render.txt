# Slim requirements for Render free-tier deployment.
# Removes PyTorch-dependent packages; uses Groq Whisper API + fastembed (ONNX) instead.

# Core
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
httpx==0.27.0
aiofiles==23.2.1
pydantic==2.7.1
pydantic-settings==2.3.1

# Database
sqlalchemy==2.0.30
alembic==1.13.1

# Task Queue
celery==5.4.0
redis==5.0.4
kombu==5.3.7

# Embeddings — ONNX-based (no PyTorch, ~130 MB model download)
fastembed==0.3.6
# numpy<2 required by faiss-cpu
numpy>=1.24.0,<2.0.0
faiss-cpu==1.8.0

# Video / Audio processing
opencv-python-headless==4.10.0.84
scenedetect[opencv]==0.6.4
yt-dlp>=2026.6.1

# PDF Export
reportlab==4.2.0
Pillow==10.3.0

# Database — PostgreSQL (Supabase)
psycopg2-binary==2.9.9

# Auth / JWT
PyJWT==2.9.0
supabase==2.5.3

# Security / Rate limiting
slowapi==0.1.9
limits==3.13.0

# Error tracking
sentry-sdk[fastapi]==2.7.1

# Utilities
python-dotenv==1.0.1
structlog==24.2.0
tenacity==8.3.0
langdetect==1.0.9

# Groq (LLM + Whisper API — transcription now via API, no local model)
groq==0.9.0

# Google Gemini (api mode)
google-generativeai==0.7.2

# Ollama (local mode)
ollama==0.2.1
